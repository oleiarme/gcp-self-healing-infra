[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: Unit tests for individual modules
    integration: Integration tests covering full processing cycle
    property: Property-based tests (hypothesis)
