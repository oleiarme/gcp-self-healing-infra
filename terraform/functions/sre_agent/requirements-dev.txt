# SRE-agent — dev/test dependencies (NOT deployed to Cloud Function)
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
hypothesis==6.115.6

# --- HTTP mocking for httpx ---
respx==0.22.0

# --- Coverage ---
pytest-cov==6.0.0
